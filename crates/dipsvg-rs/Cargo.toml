[package]
name = "dipsvg-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR GPL-3.0-or-later"

[lib]
name = "dipsvg"

[dependencies]
palcore = { path = "../palcore" }
svg = "0.18"
thiserror = { workspace = true }
toml = { workspace = true }
